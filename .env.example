# Copy to .env and fill in.
# Playwright reads these via process.env.

# Base URL under test
BASE_URL=https://www.saucedemo.com

# Test user credentials (safe demo creds for saucedemo)
TEST_USER=standard_user
TEST_PASSWORD=secret_sauce

# CI toggles
CI=false
HEADLESS=true
